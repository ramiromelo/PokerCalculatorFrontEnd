export const pl1range1R = (state = true, action) => { switch(action.type) { case 'PL1_1': return !state; default: return state; }}
export const pl1range2R = (state = true, action) => { switch(action.type) { case 'PL1_2': return !state; default: return state; }}
export const pl1range3R = (state = true, action) => { switch(action.type) { case 'PL1_3': return !state; default: return state; }}
export const pl1range4R = (state = true, action) => { switch(action.type) { case 'PL1_4': return !state; default: return state; }}
export const pl1range5R = (state = true, action) => { switch(action.type) { case 'PL1_5': return !state; default: return state; }}
export const pl1range6R = (state = true, action) => { switch(action.type) { case 'PL1_6': return !state; default: return state; }}
export const pl1range7R = (state = true, action) => { switch(action.type) { case 'PL1_7': return !state; default: return state; }}
export const pl1range8R = (state = true, action) => { switch(action.type) { case 'PL1_8': return !state; default: return state; }}
export const pl1range9R = (state = true, action) => { switch(action.type) { case 'PL1_9': return !state; default: return state; }}
export const pl1range10R = (state = true, action) => { switch(action.type) { case 'PL1_10': return !state; default: return state; }}
export const pl1range11R = (state = true, action) => { switch(action.type) { case 'PL1_11': return !state; default: return state; }}
export const pl1range12R = (state = true, action) => { switch(action.type) { case 'PL1_12': return !state; default: return state; }}
export const pl1range13R = (state = true, action) => { switch(action.type) { case 'PL1_13': return !state; default: return state; }}
export const pl1range14R = (state = true, action) => { switch(action.type) { case 'PL1_14': return !state; default: return state; }}
export const pl1range15R = (state = true, action) => { switch(action.type) { case 'PL1_15': return !state; default: return state; }}
export const pl1range16R = (state = true, action) => { switch(action.type) { case 'PL1_16': return !state; default: return state; }}
export const pl1range17R = (state = true, action) => { switch(action.type) { case 'PL1_17': return !state; default: return state; }}
export const pl1range18R = (state = true, action) => { switch(action.type) { case 'PL1_18': return !state; default: return state; }}
export const pl1range19R = (state = true, action) => { switch(action.type) { case 'PL1_19': return !state; default: return state; }}
export const pl1range20R = (state = true, action) => { switch(action.type) { case 'PL1_20': return !state; default: return state; }}
export const pl1range21R = (state = true, action) => { switch(action.type) { case 'PL1_21': return !state; default: return state; }}
export const pl1range22R = (state = true, action) => { switch(action.type) { case 'PL1_22': return !state; default: return state; }}
export const pl1range23R = (state = true, action) => { switch(action.type) { case 'PL1_23': return !state; default: return state; }}
export const pl1range24R = (state = true, action) => { switch(action.type) { case 'PL1_24': return !state; default: return state; }}
export const pl1range25R = (state = true, action) => { switch(action.type) { case 'PL1_25': return !state; default: return state; }}
export const pl1range26R = (state = true, action) => { switch(action.type) { case 'PL1_26': return !state; default: return state; }}
export const pl1range27R = (state = true, action) => { switch(action.type) { case 'PL1_27': return !state; default: return state; }}
export const pl1range28R = (state = true, action) => { switch(action.type) { case 'PL1_28': return !state; default: return state; }}
export const pl1range29R = (state = true, action) => { switch(action.type) { case 'PL1_29': return !state; default: return state; }}
export const pl1range30R = (state = true, action) => { switch(action.type) { case 'PL1_30': return !state; default: return state; }}
export const pl1range31R = (state = true, action) => { switch(action.type) { case 'PL1_31': return !state; default: return state; }}
export const pl1range32R = (state = true, action) => { switch(action.type) { case 'PL1_32': return !state; default: return state; }}
export const pl1range33R = (state = true, action) => { switch(action.type) { case 'PL1_33': return !state; default: return state; }}
export const pl1range34R = (state = true, action) => { switch(action.type) { case 'PL1_34': return !state; default: return state; }}
export const pl1range35R = (state = true, action) => { switch(action.type) { case 'PL1_35': return !state; default: return state; }}
export const pl1range36R = (state = true, action) => { switch(action.type) { case 'PL1_36': return !state; default: return state; }}
export const pl1range37R = (state = true, action) => { switch(action.type) { case 'PL1_37': return !state; default: return state; }}
export const pl1range38R = (state = true, action) => { switch(action.type) { case 'PL1_38': return !state; default: return state; }}
export const pl1range39R = (state = true, action) => { switch(action.type) { case 'PL1_39': return !state; default: return state; }}
export const pl1range40R = (state = true, action) => { switch(action.type) { case 'PL1_40': return !state; default: return state; }}
export const pl1range41R = (state = true, action) => { switch(action.type) { case 'PL1_41': return !state; default: return state; }}
export const pl1range42R = (state = true, action) => { switch(action.type) { case 'PL1_42': return !state; default: return state; }}
export const pl1range43R = (state = true, action) => { switch(action.type) { case 'PL1_43': return !state; default: return state; }}
export const pl1range44R = (state = true, action) => { switch(action.type) { case 'PL1_44': return !state; default: return state; }}
export const pl1range45R = (state = true, action) => { switch(action.type) { case 'PL1_45': return !state; default: return state; }}
export const pl1range46R = (state = true, action) => { switch(action.type) { case 'PL1_46': return !state; default: return state; }}
export const pl1range47R = (state = true, action) => { switch(action.type) { case 'PL1_47': return !state; default: return state; }}
export const pl1range48R = (state = true, action) => { switch(action.type) { case 'PL1_48': return !state; default: return state; }}
export const pl1range49R = (state = true, action) => { switch(action.type) { case 'PL1_49': return !state; default: return state; }}
export const pl1range50R = (state = true, action) => { switch(action.type) { case 'PL1_50': return !state; default: return state; }}
export const pl1range51R = (state = true, action) => { switch(action.type) { case 'PL1_51': return !state; default: return state; }}
export const pl1range52R = (state = true, action) => { switch(action.type) { case 'PL1_52': return !state; default: return state; }}
export const pl1range53R = (state = true, action) => { switch(action.type) { case 'PL1_53': return !state; default: return state; }}
export const pl1range54R = (state = true, action) => { switch(action.type) { case 'PL1_54': return !state; default: return state; }}
export const pl1range55R = (state = true, action) => { switch(action.type) { case 'PL1_55': return !state; default: return state; }}
export const pl1range56R = (state = true, action) => { switch(action.type) { case 'PL1_56': return !state; default: return state; }}
export const pl1range57R = (state = true, action) => { switch(action.type) { case 'PL1_57': return !state; default: return state; }}
export const pl1range58R = (state = true, action) => { switch(action.type) { case 'PL1_58': return !state; default: return state; }}
export const pl1range59R = (state = true, action) => { switch(action.type) { case 'PL1_59': return !state; default: return state; }}
export const pl1range60R = (state = true, action) => { switch(action.type) { case 'PL1_60': return !state; default: return state; }}
export const pl1range61R = (state = true, action) => { switch(action.type) { case 'PL1_61': return !state; default: return state; }}
export const pl1range62R = (state = true, action) => { switch(action.type) { case 'PL1_62': return !state; default: return state; }}
export const pl1range63R = (state = true, action) => { switch(action.type) { case 'PL1_63': return !state; default: return state; }}
export const pl1range64R = (state = true, action) => { switch(action.type) { case 'PL1_64': return !state; default: return state; }}
export const pl1range65R = (state = true, action) => { switch(action.type) { case 'PL1_65': return !state; default: return state; }}
export const pl1range66R = (state = true, action) => { switch(action.type) { case 'PL1_66': return !state; default: return state; }}
export const pl1range67R = (state = true, action) => { switch(action.type) { case 'PL1_67': return !state; default: return state; }}
export const pl1range68R = (state = true, action) => { switch(action.type) { case 'PL1_68': return !state; default: return state; }}
export const pl1range69R = (state = true, action) => { switch(action.type) { case 'PL1_69': return !state; default: return state; }}
export const pl1range70R = (state = true, action) => { switch(action.type) { case 'PL1_70': return !state; default: return state; }}
export const pl1range71R = (state = true, action) => { switch(action.type) { case 'PL1_71': return !state; default: return state; }}
export const pl1range72R = (state = true, action) => { switch(action.type) { case 'PL1_72': return !state; default: return state; }}
export const pl1range73R = (state = true, action) => { switch(action.type) { case 'PL1_73': return !state; default: return state; }}
export const pl1range74R = (state = true, action) => { switch(action.type) { case 'PL1_74': return !state; default: return state; }}
export const pl1range75R = (state = true, action) => { switch(action.type) { case 'PL1_75': return !state; default: return state; }}
export const pl1range76R = (state = true, action) => { switch(action.type) { case 'PL1_76': return !state; default: return state; }}
export const pl1range77R = (state = true, action) => { switch(action.type) { case 'PL1_77': return !state; default: return state; }}
export const pl1range78R = (state = true, action) => { switch(action.type) { case 'PL1_78': return !state; default: return state; }}
export const pl1range79R = (state = true, action) => { switch(action.type) { case 'PL1_79': return !state; default: return state; }}
export const pl1range80R = (state = true, action) => { switch(action.type) { case 'PL1_80': return !state; default: return state; }}
export const pl1range81R = (state = true, action) => { switch(action.type) { case 'PL1_81': return !state; default: return state; }}
export const pl1range82R = (state = true, action) => { switch(action.type) { case 'PL1_82': return !state; default: return state; }}
export const pl1range83R = (state = true, action) => { switch(action.type) { case 'PL1_83': return !state; default: return state; }}
export const pl1range84R = (state = true, action) => { switch(action.type) { case 'PL1_84': return !state; default: return state; }}
export const pl1range85R = (state = true, action) => { switch(action.type) { case 'PL1_85': return !state; default: return state; }}
export const pl1range86R = (state = true, action) => { switch(action.type) { case 'PL1_86': return !state; default: return state; }}
export const pl1range87R = (state = true, action) => { switch(action.type) { case 'PL1_87': return !state; default: return state; }}
export const pl1range88R = (state = true, action) => { switch(action.type) { case 'PL1_88': return !state; default: return state; }}
export const pl1range89R = (state = true, action) => { switch(action.type) { case 'PL1_89': return !state; default: return state; }}
export const pl1range90R = (state = true, action) => { switch(action.type) { case 'PL1_90': return !state; default: return state; }}
export const pl1range91R = (state = true, action) => { switch(action.type) { case 'PL1_91': return !state; default: return state; }}
export const pl1range92R = (state = true, action) => { switch(action.type) { case 'PL1_92': return !state; default: return state; }}
export const pl1range93R = (state = true, action) => { switch(action.type) { case 'PL1_93': return !state; default: return state; }}
export const pl1range94R = (state = true, action) => { switch(action.type) { case 'PL1_94': return !state; default: return state; }}
export const pl1range95R = (state = true, action) => { switch(action.type) { case 'PL1_95': return !state; default: return state; }}
export const pl1range96R = (state = true, action) => { switch(action.type) { case 'PL1_96': return !state; default: return state; }}
export const pl1range97R = (state = true, action) => { switch(action.type) { case 'PL1_97': return !state; default: return state; }}
export const pl1range98R = (state = true, action) => { switch(action.type) { case 'PL1_98': return !state; default: return state; }}
export const pl1range99R = (state = true, action) => { switch(action.type) { case 'PL1_99': return !state; default: return state; }}
export const pl1range100R = (state = true, action) => { switch(action.type) { case 'PL1_100': return !state; default: return state; }}
export const pl1range101R = (state = true, action) => { switch(action.type) { case 'PL1_101': return !state; default: return state; }}
export const pl1range102R = (state = true, action) => { switch(action.type) { case 'PL1_102': return !state; default: return state; }}
export const pl1range103R = (state = true, action) => { switch(action.type) { case 'PL1_103': return !state; default: return state; }}
export const pl1range104R = (state = true, action) => { switch(action.type) { case 'PL1_104': return !state; default: return state; }}
export const pl1range105R = (state = true, action) => { switch(action.type) { case 'PL1_105': return !state; default: return state; }}
export const pl1range106R = (state = true, action) => { switch(action.type) { case 'PL1_106': return !state; default: return state; }}
export const pl1range107R = (state = true, action) => { switch(action.type) { case 'PL1_107': return !state; default: return state; }}
export const pl1range108R = (state = true, action) => { switch(action.type) { case 'PL1_108': return !state; default: return state; }}
export const pl1range109R = (state = true, action) => { switch(action.type) { case 'PL1_109': return !state; default: return state; }}
export const pl1range110R = (state = true, action) => { switch(action.type) { case 'PL1_110': return !state; default: return state; }}
export const pl1range111R = (state = true, action) => { switch(action.type) { case 'PL1_111': return !state; default: return state; }}
export const pl1range112R = (state = true, action) => { switch(action.type) { case 'PL1_112': return !state; default: return state; }}
export const pl1range113R = (state = true, action) => { switch(action.type) { case 'PL1_113': return !state; default: return state; }}
export const pl1range114R = (state = true, action) => { switch(action.type) { case 'PL1_114': return !state; default: return state; }}
export const pl1range115R = (state = true, action) => { switch(action.type) { case 'PL1_115': return !state; default: return state; }}
export const pl1range116R = (state = true, action) => { switch(action.type) { case 'PL1_116': return !state; default: return state; }}
export const pl1range117R = (state = true, action) => { switch(action.type) { case 'PL1_117': return !state; default: return state; }}
export const pl1range118R = (state = true, action) => { switch(action.type) { case 'PL1_118': return !state; default: return state; }}
export const pl1range119R = (state = true, action) => { switch(action.type) { case 'PL1_119': return !state; default: return state; }}
export const pl1range120R = (state = true, action) => { switch(action.type) { case 'PL1_120': return !state; default: return state; }}
export const pl1range121R = (state = true, action) => { switch(action.type) { case 'PL1_121': return !state; default: return state; }}
export const pl1range122R = (state = true, action) => { switch(action.type) { case 'PL1_122': return !state; default: return state; }}
export const pl1range123R = (state = true, action) => { switch(action.type) { case 'PL1_123': return !state; default: return state; }}
export const pl1range124R = (state = true, action) => { switch(action.type) { case 'PL1_124': return !state; default: return state; }}
export const pl1range125R = (state = true, action) => { switch(action.type) { case 'PL1_125': return !state; default: return state; }}
export const pl1range126R = (state = true, action) => { switch(action.type) { case 'PL1_126': return !state; default: return state; }}
export const pl1range127R = (state = true, action) => { switch(action.type) { case 'PL1_127': return !state; default: return state; }}
export const pl1range128R = (state = true, action) => { switch(action.type) { case 'PL1_128': return !state; default: return state; }}
export const pl1range129R = (state = true, action) => { switch(action.type) { case 'PL1_129': return !state; default: return state; }}
export const pl1range130R = (state = true, action) => { switch(action.type) { case 'PL1_130': return !state; default: return state; }}
export const pl1range131R = (state = true, action) => { switch(action.type) { case 'PL1_131': return !state; default: return state; }}
export const pl1range132R = (state = true, action) => { switch(action.type) { case 'PL1_132': return !state; default: return state; }}
export const pl1range133R = (state = true, action) => { switch(action.type) { case 'PL1_133': return !state; default: return state; }}
export const pl1range134R = (state = true, action) => { switch(action.type) { case 'PL1_134': return !state; default: return state; }}
export const pl1range135R = (state = true, action) => { switch(action.type) { case 'PL1_135': return !state; default: return state; }}
export const pl1range136R = (state = true, action) => { switch(action.type) { case 'PL1_136': return !state; default: return state; }}
export const pl1range137R = (state = true, action) => { switch(action.type) { case 'PL1_137': return !state; default: return state; }}
export const pl1range138R = (state = true, action) => { switch(action.type) { case 'PL1_138': return !state; default: return state; }}
export const pl1range139R = (state = true, action) => { switch(action.type) { case 'PL1_139': return !state; default: return state; }}
export const pl1range140R = (state = true, action) => { switch(action.type) { case 'PL1_140': return !state; default: return state; }}
export const pl1range141R = (state = true, action) => { switch(action.type) { case 'PL1_141': return !state; default: return state; }}
export const pl1range142R = (state = true, action) => { switch(action.type) { case 'PL1_142': return !state; default: return state; }}
export const pl1range143R = (state = true, action) => { switch(action.type) { case 'PL1_143': return !state; default: return state; }}
export const pl1range144R = (state = true, action) => { switch(action.type) { case 'PL1_144': return !state; default: return state; }}
export const pl1range145R = (state = true, action) => { switch(action.type) { case 'PL1_145': return !state; default: return state; }}
export const pl1range146R = (state = true, action) => { switch(action.type) { case 'PL1_146': return !state; default: return state; }}
export const pl1range147R = (state = true, action) => { switch(action.type) { case 'PL1_147': return !state; default: return state; }}
export const pl1range148R = (state = true, action) => { switch(action.type) { case 'PL1_148': return !state; default: return state; }}
export const pl1range149R = (state = true, action) => { switch(action.type) { case 'PL1_149': return !state; default: return state; }}
export const pl1range150R = (state = true, action) => { switch(action.type) { case 'PL1_150': return !state; default: return state; }}
export const pl1range151R = (state = true, action) => { switch(action.type) { case 'PL1_151': return !state; default: return state; }}
export const pl1range152R = (state = true, action) => { switch(action.type) { case 'PL1_152': return !state; default: return state; }}
export const pl1range153R = (state = true, action) => { switch(action.type) { case 'PL1_153': return !state; default: return state; }}
export const pl1range154R = (state = true, action) => { switch(action.type) { case 'PL1_154': return !state; default: return state; }}
export const pl1range155R = (state = true, action) => { switch(action.type) { case 'PL1_155': return !state; default: return state; }}
export const pl1range156R = (state = true, action) => { switch(action.type) { case 'PL1_156': return !state; default: return state; }}
export const pl1range157R = (state = true, action) => { switch(action.type) { case 'PL1_157': return !state; default: return state; }}
export const pl1range158R = (state = true, action) => { switch(action.type) { case 'PL1_158': return !state; default: return state; }}
export const pl1range159R = (state = true, action) => { switch(action.type) { case 'PL1_159': return !state; default: return state; }}
export const pl1range160R = (state = true, action) => { switch(action.type) { case 'PL1_160': return !state; default: return state; }}
export const pl1range161R = (state = true, action) => { switch(action.type) { case 'PL1_161': return !state; default: return state; }}
export const pl1range162R = (state = true, action) => { switch(action.type) { case 'PL1_162': return !state; default: return state; }}
export const pl1range163R = (state = true, action) => { switch(action.type) { case 'PL1_163': return !state; default: return state; }}
export const pl1range164R = (state = true, action) => { switch(action.type) { case 'PL1_164': return !state; default: return state; }}
export const pl1range165R = (state = true, action) => { switch(action.type) { case 'PL1_165': return !state; default: return state; }}
export const pl1range166R = (state = true, action) => { switch(action.type) { case 'PL1_166': return !state; default: return state; }}
export const pl1range167R = (state = true, action) => { switch(action.type) { case 'PL1_167': return !state; default: return state; }}
export const pl1range168R = (state = true, action) => { switch(action.type) { case 'PL1_168': return !state; default: return state; }}
export const pl1range169R = (state = true, action) => { switch(action.type) { case 'PL1_169': return !state; default: return state; }}

export const pl2range1R = (state = true, action) => { switch(action.type) { case 'PL2_1': return !state; default: return state; }}
export const pl2range2R = (state = true, action) => { switch(action.type) { case 'PL2_2': return !state; default: return state; }}
export const pl2range3R = (state = true, action) => { switch(action.type) { case 'PL2_3': return !state; default: return state; }}
export const pl2range4R = (state = true, action) => { switch(action.type) { case 'PL2_4': return !state; default: return state; }}
export const pl2range5R = (state = true, action) => { switch(action.type) { case 'PL2_5': return !state; default: return state; }}
export const pl2range6R = (state = true, action) => { switch(action.type) { case 'PL2_6': return !state; default: return state; }}
export const pl2range7R = (state = true, action) => { switch(action.type) { case 'PL2_7': return !state; default: return state; }}
export const pl2range8R = (state = true, action) => { switch(action.type) { case 'PL2_8': return !state; default: return state; }}
export const pl2range9R = (state = true, action) => { switch(action.type) { case 'PL2_9': return !state; default: return state; }}
export const pl2range10R = (state = true, action) => { switch(action.type) { case 'PL2_10': return !state; default: return state; }}
export const pl2range11R = (state = true, action) => { switch(action.type) { case 'PL2_11': return !state; default: return state; }}
export const pl2range12R = (state = true, action) => { switch(action.type) { case 'PL2_12': return !state; default: return state; }}
export const pl2range13R = (state = true, action) => { switch(action.type) { case 'PL2_13': return !state; default: return state; }}
export const pl2range14R = (state = true, action) => { switch(action.type) { case 'PL2_14': return !state; default: return state; }}
export const pl2range15R = (state = true, action) => { switch(action.type) { case 'PL2_15': return !state; default: return state; }}
export const pl2range16R = (state = true, action) => { switch(action.type) { case 'PL2_16': return !state; default: return state; }}
export const pl2range17R = (state = true, action) => { switch(action.type) { case 'PL2_17': return !state; default: return state; }}
export const pl2range18R = (state = true, action) => { switch(action.type) { case 'PL2_18': return !state; default: return state; }}
export const pl2range19R = (state = true, action) => { switch(action.type) { case 'PL2_19': return !state; default: return state; }}
export const pl2range20R = (state = true, action) => { switch(action.type) { case 'PL2_20': return !state; default: return state; }}
export const pl2range21R = (state = true, action) => { switch(action.type) { case 'PL2_21': return !state; default: return state; }}
export const pl2range22R = (state = true, action) => { switch(action.type) { case 'PL2_22': return !state; default: return state; }}
export const pl2range23R = (state = true, action) => { switch(action.type) { case 'PL2_23': return !state; default: return state; }}
export const pl2range24R = (state = true, action) => { switch(action.type) { case 'PL2_24': return !state; default: return state; }}
export const pl2range25R = (state = true, action) => { switch(action.type) { case 'PL2_25': return !state; default: return state; }}
export const pl2range26R = (state = true, action) => { switch(action.type) { case 'PL2_26': return !state; default: return state; }}
export const pl2range27R = (state = true, action) => { switch(action.type) { case 'PL2_27': return !state; default: return state; }}
export const pl2range28R = (state = true, action) => { switch(action.type) { case 'PL2_28': return !state; default: return state; }}
export const pl2range29R = (state = true, action) => { switch(action.type) { case 'PL2_29': return !state; default: return state; }}
export const pl2range30R = (state = true, action) => { switch(action.type) { case 'PL2_30': return !state; default: return state; }}
export const pl2range31R = (state = true, action) => { switch(action.type) { case 'PL2_31': return !state; default: return state; }}
export const pl2range32R = (state = true, action) => { switch(action.type) { case 'PL2_32': return !state; default: return state; }}
export const pl2range33R = (state = true, action) => { switch(action.type) { case 'PL2_33': return !state; default: return state; }}
export const pl2range34R = (state = true, action) => { switch(action.type) { case 'PL2_34': return !state; default: return state; }}
export const pl2range35R = (state = true, action) => { switch(action.type) { case 'PL2_35': return !state; default: return state; }}
export const pl2range36R = (state = true, action) => { switch(action.type) { case 'PL2_36': return !state; default: return state; }}
export const pl2range37R = (state = true, action) => { switch(action.type) { case 'PL2_37': return !state; default: return state; }}
export const pl2range38R = (state = true, action) => { switch(action.type) { case 'PL2_38': return !state; default: return state; }}
export const pl2range39R = (state = true, action) => { switch(action.type) { case 'PL2_39': return !state; default: return state; }}
export const pl2range40R = (state = true, action) => { switch(action.type) { case 'PL2_40': return !state; default: return state; }}
export const pl2range41R = (state = true, action) => { switch(action.type) { case 'PL2_41': return !state; default: return state; }}
export const pl2range42R = (state = true, action) => { switch(action.type) { case 'PL2_42': return !state; default: return state; }}
export const pl2range43R = (state = true, action) => { switch(action.type) { case 'PL2_43': return !state; default: return state; }}
export const pl2range44R = (state = true, action) => { switch(action.type) { case 'PL2_44': return !state; default: return state; }}
export const pl2range45R = (state = true, action) => { switch(action.type) { case 'PL2_45': return !state; default: return state; }}
export const pl2range46R = (state = true, action) => { switch(action.type) { case 'PL2_46': return !state; default: return state; }}
export const pl2range47R = (state = true, action) => { switch(action.type) { case 'PL2_47': return !state; default: return state; }}
export const pl2range48R = (state = true, action) => { switch(action.type) { case 'PL2_48': return !state; default: return state; }}
export const pl2range49R = (state = true, action) => { switch(action.type) { case 'PL2_49': return !state; default: return state; }}
export const pl2range50R = (state = true, action) => { switch(action.type) { case 'PL2_50': return !state; default: return state; }}
export const pl2range51R = (state = true, action) => { switch(action.type) { case 'PL2_51': return !state; default: return state; }}
export const pl2range52R = (state = true, action) => { switch(action.type) { case 'PL2_52': return !state; default: return state; }}
export const pl2range53R = (state = true, action) => { switch(action.type) { case 'PL2_53': return !state; default: return state; }}
export const pl2range54R = (state = true, action) => { switch(action.type) { case 'PL2_54': return !state; default: return state; }}
export const pl2range55R = (state = true, action) => { switch(action.type) { case 'PL2_55': return !state; default: return state; }}
export const pl2range56R = (state = true, action) => { switch(action.type) { case 'PL2_56': return !state; default: return state; }}
export const pl2range57R = (state = true, action) => { switch(action.type) { case 'PL2_57': return !state; default: return state; }}
export const pl2range58R = (state = true, action) => { switch(action.type) { case 'PL2_58': return !state; default: return state; }}
export const pl2range59R = (state = true, action) => { switch(action.type) { case 'PL2_59': return !state; default: return state; }}
export const pl2range60R = (state = true, action) => { switch(action.type) { case 'PL2_60': return !state; default: return state; }}
export const pl2range61R = (state = true, action) => { switch(action.type) { case 'PL2_61': return !state; default: return state; }}
export const pl2range62R = (state = true, action) => { switch(action.type) { case 'PL2_62': return !state; default: return state; }}
export const pl2range63R = (state = true, action) => { switch(action.type) { case 'PL2_63': return !state; default: return state; }}
export const pl2range64R = (state = true, action) => { switch(action.type) { case 'PL2_64': return !state; default: return state; }}
export const pl2range65R = (state = true, action) => { switch(action.type) { case 'PL2_65': return !state; default: return state; }}
export const pl2range66R = (state = true, action) => { switch(action.type) { case 'PL2_66': return !state; default: return state; }}
export const pl2range67R = (state = true, action) => { switch(action.type) { case 'PL2_67': return !state; default: return state; }}
export const pl2range68R = (state = true, action) => { switch(action.type) { case 'PL2_68': return !state; default: return state; }}
export const pl2range69R = (state = true, action) => { switch(action.type) { case 'PL2_69': return !state; default: return state; }}
export const pl2range70R = (state = true, action) => { switch(action.type) { case 'PL2_70': return !state; default: return state; }}
export const pl2range71R = (state = true, action) => { switch(action.type) { case 'PL2_71': return !state; default: return state; }}
export const pl2range72R = (state = true, action) => { switch(action.type) { case 'PL2_72': return !state; default: return state; }}
export const pl2range73R = (state = true, action) => { switch(action.type) { case 'PL2_73': return !state; default: return state; }}
export const pl2range74R = (state = true, action) => { switch(action.type) { case 'PL2_74': return !state; default: return state; }}
export const pl2range75R = (state = true, action) => { switch(action.type) { case 'PL2_75': return !state; default: return state; }}
export const pl2range76R = (state = true, action) => { switch(action.type) { case 'PL2_76': return !state; default: return state; }}
export const pl2range77R = (state = true, action) => { switch(action.type) { case 'PL2_77': return !state; default: return state; }}
export const pl2range78R = (state = true, action) => { switch(action.type) { case 'PL2_78': return !state; default: return state; }}
export const pl2range79R = (state = true, action) => { switch(action.type) { case 'PL2_79': return !state; default: return state; }}
export const pl2range80R = (state = true, action) => { switch(action.type) { case 'PL2_80': return !state; default: return state; }}
export const pl2range81R = (state = true, action) => { switch(action.type) { case 'PL2_81': return !state; default: return state; }}
export const pl2range82R = (state = true, action) => { switch(action.type) { case 'PL2_82': return !state; default: return state; }}
export const pl2range83R = (state = true, action) => { switch(action.type) { case 'PL2_83': return !state; default: return state; }}
export const pl2range84R = (state = true, action) => { switch(action.type) { case 'PL2_84': return !state; default: return state; }}
export const pl2range85R = (state = true, action) => { switch(action.type) { case 'PL2_85': return !state; default: return state; }}
export const pl2range86R = (state = true, action) => { switch(action.type) { case 'PL2_86': return !state; default: return state; }}
export const pl2range87R = (state = true, action) => { switch(action.type) { case 'PL2_87': return !state; default: return state; }}
export const pl2range88R = (state = true, action) => { switch(action.type) { case 'PL2_88': return !state; default: return state; }}
export const pl2range89R = (state = true, action) => { switch(action.type) { case 'PL2_89': return !state; default: return state; }}
export const pl2range90R = (state = true, action) => { switch(action.type) { case 'PL2_90': return !state; default: return state; }}
export const pl2range91R = (state = true, action) => { switch(action.type) { case 'PL2_91': return !state; default: return state; }}
export const pl2range92R = (state = true, action) => { switch(action.type) { case 'PL2_92': return !state; default: return state; }}
export const pl2range93R = (state = true, action) => { switch(action.type) { case 'PL2_93': return !state; default: return state; }}
export const pl2range94R = (state = true, action) => { switch(action.type) { case 'PL2_94': return !state; default: return state; }}
export const pl2range95R = (state = true, action) => { switch(action.type) { case 'PL2_95': return !state; default: return state; }}
export const pl2range96R = (state = true, action) => { switch(action.type) { case 'PL2_96': return !state; default: return state; }}
export const pl2range97R = (state = true, action) => { switch(action.type) { case 'PL2_97': return !state; default: return state; }}
export const pl2range98R = (state = true, action) => { switch(action.type) { case 'PL2_98': return !state; default: return state; }}
export const pl2range99R = (state = true, action) => { switch(action.type) { case 'PL2_99': return !state; default: return state; }}
export const pl2range100R = (state = true, action) => { switch(action.type) { case 'PL2_100': return !state; default: return state; }}
export const pl2range101R = (state = true, action) => { switch(action.type) { case 'PL2_101': return !state; default: return state; }}
export const pl2range102R = (state = true, action) => { switch(action.type) { case 'PL2_102': return !state; default: return state; }}
export const pl2range103R = (state = true, action) => { switch(action.type) { case 'PL2_103': return !state; default: return state; }}
export const pl2range104R = (state = true, action) => { switch(action.type) { case 'PL2_104': return !state; default: return state; }}
export const pl2range105R = (state = true, action) => { switch(action.type) { case 'PL2_105': return !state; default: return state; }}
export const pl2range106R = (state = true, action) => { switch(action.type) { case 'PL2_106': return !state; default: return state; }}
export const pl2range107R = (state = true, action) => { switch(action.type) { case 'PL2_107': return !state; default: return state; }}
export const pl2range108R = (state = true, action) => { switch(action.type) { case 'PL2_108': return !state; default: return state; }}
export const pl2range109R = (state = true, action) => { switch(action.type) { case 'PL2_109': return !state; default: return state; }}
export const pl2range110R = (state = true, action) => { switch(action.type) { case 'PL2_110': return !state; default: return state; }}
export const pl2range111R = (state = true, action) => { switch(action.type) { case 'PL2_111': return !state; default: return state; }}
export const pl2range112R = (state = true, action) => { switch(action.type) { case 'PL2_112': return !state; default: return state; }}
export const pl2range113R = (state = true, action) => { switch(action.type) { case 'PL2_113': return !state; default: return state; }}
export const pl2range114R = (state = true, action) => { switch(action.type) { case 'PL2_114': return !state; default: return state; }}
export const pl2range115R = (state = true, action) => { switch(action.type) { case 'PL2_115': return !state; default: return state; }}
export const pl2range116R = (state = true, action) => { switch(action.type) { case 'PL2_116': return !state; default: return state; }}
export const pl2range117R = (state = true, action) => { switch(action.type) { case 'PL2_117': return !state; default: return state; }}
export const pl2range118R = (state = true, action) => { switch(action.type) { case 'PL2_118': return !state; default: return state; }}
export const pl2range119R = (state = true, action) => { switch(action.type) { case 'PL2_119': return !state; default: return state; }}
export const pl2range120R = (state = true, action) => { switch(action.type) { case 'PL2_120': return !state; default: return state; }}
export const pl2range121R = (state = true, action) => { switch(action.type) { case 'PL2_121': return !state; default: return state; }}
export const pl2range122R = (state = true, action) => { switch(action.type) { case 'PL2_122': return !state; default: return state; }}
export const pl2range123R = (state = true, action) => { switch(action.type) { case 'PL2_123': return !state; default: return state; }}
export const pl2range124R = (state = true, action) => { switch(action.type) { case 'PL2_124': return !state; default: return state; }}
export const pl2range125R = (state = true, action) => { switch(action.type) { case 'PL2_125': return !state; default: return state; }}
export const pl2range126R = (state = true, action) => { switch(action.type) { case 'PL2_126': return !state; default: return state; }}
export const pl2range127R = (state = true, action) => { switch(action.type) { case 'PL2_127': return !state; default: return state; }}
export const pl2range128R = (state = true, action) => { switch(action.type) { case 'PL2_128': return !state; default: return state; }}
export const pl2range129R = (state = true, action) => { switch(action.type) { case 'PL2_129': return !state; default: return state; }}
export const pl2range130R = (state = true, action) => { switch(action.type) { case 'PL2_130': return !state; default: return state; }}
export const pl2range131R = (state = true, action) => { switch(action.type) { case 'PL2_131': return !state; default: return state; }}
export const pl2range132R = (state = true, action) => { switch(action.type) { case 'PL2_132': return !state; default: return state; }}
export const pl2range133R = (state = true, action) => { switch(action.type) { case 'PL2_133': return !state; default: return state; }}
export const pl2range134R = (state = true, action) => { switch(action.type) { case 'PL2_134': return !state; default: return state; }}
export const pl2range135R = (state = true, action) => { switch(action.type) { case 'PL2_135': return !state; default: return state; }}
export const pl2range136R = (state = true, action) => { switch(action.type) { case 'PL2_136': return !state; default: return state; }}
export const pl2range137R = (state = true, action) => { switch(action.type) { case 'PL2_137': return !state; default: return state; }}
export const pl2range138R = (state = true, action) => { switch(action.type) { case 'PL2_138': return !state; default: return state; }}
export const pl2range139R = (state = true, action) => { switch(action.type) { case 'PL2_139': return !state; default: return state; }}
export const pl2range140R = (state = true, action) => { switch(action.type) { case 'PL2_140': return !state; default: return state; }}
export const pl2range141R = (state = true, action) => { switch(action.type) { case 'PL2_141': return !state; default: return state; }}
export const pl2range142R = (state = true, action) => { switch(action.type) { case 'PL2_142': return !state; default: return state; }}
export const pl2range143R = (state = true, action) => { switch(action.type) { case 'PL2_143': return !state; default: return state; }}
export const pl2range144R = (state = true, action) => { switch(action.type) { case 'PL2_144': return !state; default: return state; }}
export const pl2range145R = (state = true, action) => { switch(action.type) { case 'PL2_145': return !state; default: return state; }}
export const pl2range146R = (state = true, action) => { switch(action.type) { case 'PL2_146': return !state; default: return state; }}
export const pl2range147R = (state = true, action) => { switch(action.type) { case 'PL2_147': return !state; default: return state; }}
export const pl2range148R = (state = true, action) => { switch(action.type) { case 'PL2_148': return !state; default: return state; }}
export const pl2range149R = (state = true, action) => { switch(action.type) { case 'PL2_149': return !state; default: return state; }}
export const pl2range150R = (state = true, action) => { switch(action.type) { case 'PL2_150': return !state; default: return state; }}
export const pl2range151R = (state = true, action) => { switch(action.type) { case 'PL2_151': return !state; default: return state; }}
export const pl2range152R = (state = true, action) => { switch(action.type) { case 'PL2_152': return !state; default: return state; }}
export const pl2range153R = (state = true, action) => { switch(action.type) { case 'PL2_153': return !state; default: return state; }}
export const pl2range154R = (state = true, action) => { switch(action.type) { case 'PL2_154': return !state; default: return state; }}
export const pl2range155R = (state = true, action) => { switch(action.type) { case 'PL2_155': return !state; default: return state; }}
export const pl2range156R = (state = true, action) => { switch(action.type) { case 'PL2_156': return !state; default: return state; }}
export const pl2range157R = (state = true, action) => { switch(action.type) { case 'PL2_157': return !state; default: return state; }}
export const pl2range158R = (state = true, action) => { switch(action.type) { case 'PL2_158': return !state; default: return state; }}
export const pl2range159R = (state = true, action) => { switch(action.type) { case 'PL2_159': return !state; default: return state; }}
export const pl2range160R = (state = true, action) => { switch(action.type) { case 'PL2_160': return !state; default: return state; }}
export const pl2range161R = (state = true, action) => { switch(action.type) { case 'PL2_161': return !state; default: return state; }}
export const pl2range162R = (state = true, action) => { switch(action.type) { case 'PL2_162': return !state; default: return state; }}
export const pl2range163R = (state = true, action) => { switch(action.type) { case 'PL2_163': return !state; default: return state; }}
export const pl2range164R = (state = true, action) => { switch(action.type) { case 'PL2_164': return !state; default: return state; }}
export const pl2range165R = (state = true, action) => { switch(action.type) { case 'PL2_165': return !state; default: return state; }}
export const pl2range166R = (state = true, action) => { switch(action.type) { case 'PL2_166': return !state; default: return state; }}
export const pl2range167R = (state = true, action) => { switch(action.type) { case 'PL2_167': return !state; default: return state; }}
export const pl2range168R = (state = true, action) => { switch(action.type) { case 'PL2_168': return !state; default: return state; }}
export const pl2range169R = (state = true, action) => { switch(action.type) { case 'PL2_169': return !state; default: return state; }}

export const pl3range1R = (state = true, action) => { switch(action.type) { case 'PL3_1': return !state; default: return state; }}
export const pl3range2R = (state = true, action) => { switch(action.type) { case 'PL3_2': return !state; default: return state; }}
export const pl3range3R = (state = true, action) => { switch(action.type) { case 'PL3_3': return !state; default: return state; }}
export const pl3range4R = (state = true, action) => { switch(action.type) { case 'PL3_4': return !state; default: return state; }}
export const pl3range5R = (state = true, action) => { switch(action.type) { case 'PL3_5': return !state; default: return state; }}
export const pl3range6R = (state = true, action) => { switch(action.type) { case 'PL3_6': return !state; default: return state; }}
export const pl3range7R = (state = true, action) => { switch(action.type) { case 'PL3_7': return !state; default: return state; }}
export const pl3range8R = (state = true, action) => { switch(action.type) { case 'PL3_8': return !state; default: return state; }}
export const pl3range9R = (state = true, action) => { switch(action.type) { case 'PL3_9': return !state; default: return state; }}
export const pl3range10R = (state = true, action) => { switch(action.type) { case 'PL3_10': return !state; default: return state; }}
export const pl3range11R = (state = true, action) => { switch(action.type) { case 'PL3_11': return !state; default: return state; }}
export const pl3range12R = (state = true, action) => { switch(action.type) { case 'PL3_12': return !state; default: return state; }}
export const pl3range13R = (state = true, action) => { switch(action.type) { case 'PL3_13': return !state; default: return state; }}
export const pl3range14R = (state = true, action) => { switch(action.type) { case 'PL3_14': return !state; default: return state; }}
export const pl3range15R = (state = true, action) => { switch(action.type) { case 'PL3_15': return !state; default: return state; }}
export const pl3range16R = (state = true, action) => { switch(action.type) { case 'PL3_16': return !state; default: return state; }}
export const pl3range17R = (state = true, action) => { switch(action.type) { case 'PL3_17': return !state; default: return state; }}
export const pl3range18R = (state = true, action) => { switch(action.type) { case 'PL3_18': return !state; default: return state; }}
export const pl3range19R = (state = true, action) => { switch(action.type) { case 'PL3_19': return !state; default: return state; }}
export const pl3range20R = (state = true, action) => { switch(action.type) { case 'PL3_20': return !state; default: return state; }}
export const pl3range21R = (state = true, action) => { switch(action.type) { case 'PL3_21': return !state; default: return state; }}
export const pl3range22R = (state = true, action) => { switch(action.type) { case 'PL3_22': return !state; default: return state; }}
export const pl3range23R = (state = true, action) => { switch(action.type) { case 'PL3_23': return !state; default: return state; }}
export const pl3range24R = (state = true, action) => { switch(action.type) { case 'PL3_24': return !state; default: return state; }}
export const pl3range25R = (state = true, action) => { switch(action.type) { case 'PL3_25': return !state; default: return state; }}
export const pl3range26R = (state = true, action) => { switch(action.type) { case 'PL3_26': return !state; default: return state; }}
export const pl3range27R = (state = true, action) => { switch(action.type) { case 'PL3_27': return !state; default: return state; }}
export const pl3range28R = (state = true, action) => { switch(action.type) { case 'PL3_28': return !state; default: return state; }}
export const pl3range29R = (state = true, action) => { switch(action.type) { case 'PL3_29': return !state; default: return state; }}
export const pl3range30R = (state = true, action) => { switch(action.type) { case 'PL3_30': return !state; default: return state; }}
export const pl3range31R = (state = true, action) => { switch(action.type) { case 'PL3_31': return !state; default: return state; }}
export const pl3range32R = (state = true, action) => { switch(action.type) { case 'PL3_32': return !state; default: return state; }}
export const pl3range33R = (state = true, action) => { switch(action.type) { case 'PL3_33': return !state; default: return state; }}
export const pl3range34R = (state = true, action) => { switch(action.type) { case 'PL3_34': return !state; default: return state; }}
export const pl3range35R = (state = true, action) => { switch(action.type) { case 'PL3_35': return !state; default: return state; }}
export const pl3range36R = (state = true, action) => { switch(action.type) { case 'PL3_36': return !state; default: return state; }}
export const pl3range37R = (state = true, action) => { switch(action.type) { case 'PL3_37': return !state; default: return state; }}
export const pl3range38R = (state = true, action) => { switch(action.type) { case 'PL3_38': return !state; default: return state; }}
export const pl3range39R = (state = true, action) => { switch(action.type) { case 'PL3_39': return !state; default: return state; }}
export const pl3range40R = (state = true, action) => { switch(action.type) { case 'PL3_40': return !state; default: return state; }}
export const pl3range41R = (state = true, action) => { switch(action.type) { case 'PL3_41': return !state; default: return state; }}
export const pl3range42R = (state = true, action) => { switch(action.type) { case 'PL3_42': return !state; default: return state; }}
export const pl3range43R = (state = true, action) => { switch(action.type) { case 'PL3_43': return !state; default: return state; }}
export const pl3range44R = (state = true, action) => { switch(action.type) { case 'PL3_44': return !state; default: return state; }}
export const pl3range45R = (state = true, action) => { switch(action.type) { case 'PL3_45': return !state; default: return state; }}
export const pl3range46R = (state = true, action) => { switch(action.type) { case 'PL3_46': return !state; default: return state; }}
export const pl3range47R = (state = true, action) => { switch(action.type) { case 'PL3_47': return !state; default: return state; }}
export const pl3range48R = (state = true, action) => { switch(action.type) { case 'PL3_48': return !state; default: return state; }}
export const pl3range49R = (state = true, action) => { switch(action.type) { case 'PL3_49': return !state; default: return state; }}
export const pl3range50R = (state = true, action) => { switch(action.type) { case 'PL3_50': return !state; default: return state; }}
export const pl3range51R = (state = true, action) => { switch(action.type) { case 'PL3_51': return !state; default: return state; }}
export const pl3range52R = (state = true, action) => { switch(action.type) { case 'PL3_52': return !state; default: return state; }}
export const pl3range53R = (state = true, action) => { switch(action.type) { case 'PL3_53': return !state; default: return state; }}
export const pl3range54R = (state = true, action) => { switch(action.type) { case 'PL3_54': return !state; default: return state; }}
export const pl3range55R = (state = true, action) => { switch(action.type) { case 'PL3_55': return !state; default: return state; }}
export const pl3range56R = (state = true, action) => { switch(action.type) { case 'PL3_56': return !state; default: return state; }}
export const pl3range57R = (state = true, action) => { switch(action.type) { case 'PL3_57': return !state; default: return state; }}
export const pl3range58R = (state = true, action) => { switch(action.type) { case 'PL3_58': return !state; default: return state; }}
export const pl3range59R = (state = true, action) => { switch(action.type) { case 'PL3_59': return !state; default: return state; }}
export const pl3range60R = (state = true, action) => { switch(action.type) { case 'PL3_60': return !state; default: return state; }}
export const pl3range61R = (state = true, action) => { switch(action.type) { case 'PL3_61': return !state; default: return state; }}
export const pl3range62R = (state = true, action) => { switch(action.type) { case 'PL3_62': return !state; default: return state; }}
export const pl3range63R = (state = true, action) => { switch(action.type) { case 'PL3_63': return !state; default: return state; }}
export const pl3range64R = (state = true, action) => { switch(action.type) { case 'PL3_64': return !state; default: return state; }}
export const pl3range65R = (state = true, action) => { switch(action.type) { case 'PL3_65': return !state; default: return state; }}
export const pl3range66R = (state = true, action) => { switch(action.type) { case 'PL3_66': return !state; default: return state; }}
export const pl3range67R = (state = true, action) => { switch(action.type) { case 'PL3_67': return !state; default: return state; }}
export const pl3range68R = (state = true, action) => { switch(action.type) { case 'PL3_68': return !state; default: return state; }}
export const pl3range69R = (state = true, action) => { switch(action.type) { case 'PL3_69': return !state; default: return state; }}
export const pl3range70R = (state = true, action) => { switch(action.type) { case 'PL3_70': return !state; default: return state; }}
export const pl3range71R = (state = true, action) => { switch(action.type) { case 'PL3_71': return !state; default: return state; }}
export const pl3range72R = (state = true, action) => { switch(action.type) { case 'PL3_72': return !state; default: return state; }}
export const pl3range73R = (state = true, action) => { switch(action.type) { case 'PL3_73': return !state; default: return state; }}
export const pl3range74R = (state = true, action) => { switch(action.type) { case 'PL3_74': return !state; default: return state; }}
export const pl3range75R = (state = true, action) => { switch(action.type) { case 'PL3_75': return !state; default: return state; }}
export const pl3range76R = (state = true, action) => { switch(action.type) { case 'PL3_76': return !state; default: return state; }}
export const pl3range77R = (state = true, action) => { switch(action.type) { case 'PL3_77': return !state; default: return state; }}
export const pl3range78R = (state = true, action) => { switch(action.type) { case 'PL3_78': return !state; default: return state; }}
export const pl3range79R = (state = true, action) => { switch(action.type) { case 'PL3_79': return !state; default: return state; }}
export const pl3range80R = (state = true, action) => { switch(action.type) { case 'PL3_80': return !state; default: return state; }}
export const pl3range81R = (state = true, action) => { switch(action.type) { case 'PL3_81': return !state; default: return state; }}
export const pl3range82R = (state = true, action) => { switch(action.type) { case 'PL3_82': return !state; default: return state; }}
export const pl3range83R = (state = true, action) => { switch(action.type) { case 'PL3_83': return !state; default: return state; }}
export const pl3range84R = (state = true, action) => { switch(action.type) { case 'PL3_84': return !state; default: return state; }}
export const pl3range85R = (state = true, action) => { switch(action.type) { case 'PL3_85': return !state; default: return state; }}
export const pl3range86R = (state = true, action) => { switch(action.type) { case 'PL3_86': return !state; default: return state; }}
export const pl3range87R = (state = true, action) => { switch(action.type) { case 'PL3_87': return !state; default: return state; }}
export const pl3range88R = (state = true, action) => { switch(action.type) { case 'PL3_88': return !state; default: return state; }}
export const pl3range89R = (state = true, action) => { switch(action.type) { case 'PL3_89': return !state; default: return state; }}
export const pl3range90R = (state = true, action) => { switch(action.type) { case 'PL3_90': return !state; default: return state; }}
export const pl3range91R = (state = true, action) => { switch(action.type) { case 'PL3_91': return !state; default: return state; }}
export const pl3range92R = (state = true, action) => { switch(action.type) { case 'PL3_92': return !state; default: return state; }}
export const pl3range93R = (state = true, action) => { switch(action.type) { case 'PL3_93': return !state; default: return state; }}
export const pl3range94R = (state = true, action) => { switch(action.type) { case 'PL3_94': return !state; default: return state; }}
export const pl3range95R = (state = true, action) => { switch(action.type) { case 'PL3_95': return !state; default: return state; }}
export const pl3range96R = (state = true, action) => { switch(action.type) { case 'PL3_96': return !state; default: return state; }}
export const pl3range97R = (state = true, action) => { switch(action.type) { case 'PL3_97': return !state; default: return state; }}
export const pl3range98R = (state = true, action) => { switch(action.type) { case 'PL3_98': return !state; default: return state; }}
export const pl3range99R = (state = true, action) => { switch(action.type) { case 'PL3_99': return !state; default: return state; }}
export const pl3range100R = (state = true, action) => { switch(action.type) { case 'PL3_100': return !state; default: return state; }}
export const pl3range101R = (state = true, action) => { switch(action.type) { case 'PL3_101': return !state; default: return state; }}
export const pl3range102R = (state = true, action) => { switch(action.type) { case 'PL3_102': return !state; default: return state; }}
export const pl3range103R = (state = true, action) => { switch(action.type) { case 'PL3_103': return !state; default: return state; }}
export const pl3range104R = (state = true, action) => { switch(action.type) { case 'PL3_104': return !state; default: return state; }}
export const pl3range105R = (state = true, action) => { switch(action.type) { case 'PL3_105': return !state; default: return state; }}
export const pl3range106R = (state = true, action) => { switch(action.type) { case 'PL3_106': return !state; default: return state; }}
export const pl3range107R = (state = true, action) => { switch(action.type) { case 'PL3_107': return !state; default: return state; }}
export const pl3range108R = (state = true, action) => { switch(action.type) { case 'PL3_108': return !state; default: return state; }}
export const pl3range109R = (state = true, action) => { switch(action.type) { case 'PL3_109': return !state; default: return state; }}
export const pl3range110R = (state = true, action) => { switch(action.type) { case 'PL3_110': return !state; default: return state; }}
export const pl3range111R = (state = true, action) => { switch(action.type) { case 'PL3_111': return !state; default: return state; }}
export const pl3range112R = (state = true, action) => { switch(action.type) { case 'PL3_112': return !state; default: return state; }}
export const pl3range113R = (state = true, action) => { switch(action.type) { case 'PL3_113': return !state; default: return state; }}
export const pl3range114R = (state = true, action) => { switch(action.type) { case 'PL3_114': return !state; default: return state; }}
export const pl3range115R = (state = true, action) => { switch(action.type) { case 'PL3_115': return !state; default: return state; }}
export const pl3range116R = (state = true, action) => { switch(action.type) { case 'PL3_116': return !state; default: return state; }}
export const pl3range117R = (state = true, action) => { switch(action.type) { case 'PL3_117': return !state; default: return state; }}
export const pl3range118R = (state = true, action) => { switch(action.type) { case 'PL3_118': return !state; default: return state; }}
export const pl3range119R = (state = true, action) => { switch(action.type) { case 'PL3_119': return !state; default: return state; }}
export const pl3range120R = (state = true, action) => { switch(action.type) { case 'PL3_120': return !state; default: return state; }}
export const pl3range121R = (state = true, action) => { switch(action.type) { case 'PL3_121': return !state; default: return state; }}
export const pl3range122R = (state = true, action) => { switch(action.type) { case 'PL3_122': return !state; default: return state; }}
export const pl3range123R = (state = true, action) => { switch(action.type) { case 'PL3_123': return !state; default: return state; }}
export const pl3range124R = (state = true, action) => { switch(action.type) { case 'PL3_124': return !state; default: return state; }}
export const pl3range125R = (state = true, action) => { switch(action.type) { case 'PL3_125': return !state; default: return state; }}
export const pl3range126R = (state = true, action) => { switch(action.type) { case 'PL3_126': return !state; default: return state; }}
export const pl3range127R = (state = true, action) => { switch(action.type) { case 'PL3_127': return !state; default: return state; }}
export const pl3range128R = (state = true, action) => { switch(action.type) { case 'PL3_128': return !state; default: return state; }}
export const pl3range129R = (state = true, action) => { switch(action.type) { case 'PL3_129': return !state; default: return state; }}
export const pl3range130R = (state = true, action) => { switch(action.type) { case 'PL3_130': return !state; default: return state; }}
export const pl3range131R = (state = true, action) => { switch(action.type) { case 'PL3_131': return !state; default: return state; }}
export const pl3range132R = (state = true, action) => { switch(action.type) { case 'PL3_132': return !state; default: return state; }}
export const pl3range133R = (state = true, action) => { switch(action.type) { case 'PL3_133': return !state; default: return state; }}
export const pl3range134R = (state = true, action) => { switch(action.type) { case 'PL3_134': return !state; default: return state; }}
export const pl3range135R = (state = true, action) => { switch(action.type) { case 'PL3_135': return !state; default: return state; }}
export const pl3range136R = (state = true, action) => { switch(action.type) { case 'PL3_136': return !state; default: return state; }}
export const pl3range137R = (state = true, action) => { switch(action.type) { case 'PL3_137': return !state; default: return state; }}
export const pl3range138R = (state = true, action) => { switch(action.type) { case 'PL3_138': return !state; default: return state; }}
export const pl3range139R = (state = true, action) => { switch(action.type) { case 'PL3_139': return !state; default: return state; }}
export const pl3range140R = (state = true, action) => { switch(action.type) { case 'PL3_140': return !state; default: return state; }}
export const pl3range141R = (state = true, action) => { switch(action.type) { case 'PL3_141': return !state; default: return state; }}
export const pl3range142R = (state = true, action) => { switch(action.type) { case 'PL3_142': return !state; default: return state; }}
export const pl3range143R = (state = true, action) => { switch(action.type) { case 'PL3_143': return !state; default: return state; }}
export const pl3range144R = (state = true, action) => { switch(action.type) { case 'PL3_144': return !state; default: return state; }}
export const pl3range145R = (state = true, action) => { switch(action.type) { case 'PL3_145': return !state; default: return state; }}
export const pl3range146R = (state = true, action) => { switch(action.type) { case 'PL3_146': return !state; default: return state; }}
export const pl3range147R = (state = true, action) => { switch(action.type) { case 'PL3_147': return !state; default: return state; }}
export const pl3range148R = (state = true, action) => { switch(action.type) { case 'PL3_148': return !state; default: return state; }}
export const pl3range149R = (state = true, action) => { switch(action.type) { case 'PL3_149': return !state; default: return state; }}
export const pl3range150R = (state = true, action) => { switch(action.type) { case 'PL3_150': return !state; default: return state; }}
export const pl3range151R = (state = true, action) => { switch(action.type) { case 'PL3_151': return !state; default: return state; }}
export const pl3range152R = (state = true, action) => { switch(action.type) { case 'PL3_152': return !state; default: return state; }}
export const pl3range153R = (state = true, action) => { switch(action.type) { case 'PL3_153': return !state; default: return state; }}
export const pl3range154R = (state = true, action) => { switch(action.type) { case 'PL3_154': return !state; default: return state; }}
export const pl3range155R = (state = true, action) => { switch(action.type) { case 'PL3_155': return !state; default: return state; }}
export const pl3range156R = (state = true, action) => { switch(action.type) { case 'PL3_156': return !state; default: return state; }}
export const pl3range157R = (state = true, action) => { switch(action.type) { case 'PL3_157': return !state; default: return state; }}
export const pl3range158R = (state = true, action) => { switch(action.type) { case 'PL3_158': return !state; default: return state; }}
export const pl3range159R = (state = true, action) => { switch(action.type) { case 'PL3_159': return !state; default: return state; }}
export const pl3range160R = (state = true, action) => { switch(action.type) { case 'PL3_160': return !state; default: return state; }}
export const pl3range161R = (state = true, action) => { switch(action.type) { case 'PL3_161': return !state; default: return state; }}
export const pl3range162R = (state = true, action) => { switch(action.type) { case 'PL3_162': return !state; default: return state; }}
export const pl3range163R = (state = true, action) => { switch(action.type) { case 'PL3_163': return !state; default: return state; }}
export const pl3range164R = (state = true, action) => { switch(action.type) { case 'PL3_164': return !state; default: return state; }}
export const pl3range165R = (state = true, action) => { switch(action.type) { case 'PL3_165': return !state; default: return state; }}
export const pl3range166R = (state = true, action) => { switch(action.type) { case 'PL3_166': return !state; default: return state; }}
export const pl3range167R = (state = true, action) => { switch(action.type) { case 'PL3_167': return !state; default: return state; }}
export const pl3range168R = (state = true, action) => { switch(action.type) { case 'PL3_168': return !state; default: return state; }}
export const pl3range169R = (state = true, action) => { switch(action.type) { case 'PL3_169': return !state; default: return state; }}

export const pl4range1R = (state = true, action) => { switch(action.type) { case 'PL4_1': return !state; default: return state; }}
export const pl4range2R = (state = true, action) => { switch(action.type) { case 'PL4_2': return !state; default: return state; }}
export const pl4range3R = (state = true, action) => { switch(action.type) { case 'PL4_3': return !state; default: return state; }}
export const pl4range4R = (state = true, action) => { switch(action.type) { case 'PL4_4': return !state; default: return state; }}
export const pl4range5R = (state = true, action) => { switch(action.type) { case 'PL4_5': return !state; default: return state; }}
export const pl4range6R = (state = true, action) => { switch(action.type) { case 'PL4_6': return !state; default: return state; }}
export const pl4range7R = (state = true, action) => { switch(action.type) { case 'PL4_7': return !state; default: return state; }}
export const pl4range8R = (state = true, action) => { switch(action.type) { case 'PL4_8': return !state; default: return state; }}
export const pl4range9R = (state = true, action) => { switch(action.type) { case 'PL4_9': return !state; default: return state; }}
export const pl4range10R = (state = true, action) => { switch(action.type) { case 'PL4_10': return !state; default: return state; }}
export const pl4range11R = (state = true, action) => { switch(action.type) { case 'PL4_11': return !state; default: return state; }}
export const pl4range12R = (state = true, action) => { switch(action.type) { case 'PL4_12': return !state; default: return state; }}
export const pl4range13R = (state = true, action) => { switch(action.type) { case 'PL4_13': return !state; default: return state; }}
export const pl4range14R = (state = true, action) => { switch(action.type) { case 'PL4_14': return !state; default: return state; }}
export const pl4range15R = (state = true, action) => { switch(action.type) { case 'PL4_15': return !state; default: return state; }}
export const pl4range16R = (state = true, action) => { switch(action.type) { case 'PL4_16': return !state; default: return state; }}
export const pl4range17R = (state = true, action) => { switch(action.type) { case 'PL4_17': return !state; default: return state; }}
export const pl4range18R = (state = true, action) => { switch(action.type) { case 'PL4_18': return !state; default: return state; }}
export const pl4range19R = (state = true, action) => { switch(action.type) { case 'PL4_19': return !state; default: return state; }}
export const pl4range20R = (state = true, action) => { switch(action.type) { case 'PL4_20': return !state; default: return state; }}
export const pl4range21R = (state = true, action) => { switch(action.type) { case 'PL4_21': return !state; default: return state; }}
export const pl4range22R = (state = true, action) => { switch(action.type) { case 'PL4_22': return !state; default: return state; }}
export const pl4range23R = (state = true, action) => { switch(action.type) { case 'PL4_23': return !state; default: return state; }}
export const pl4range24R = (state = true, action) => { switch(action.type) { case 'PL4_24': return !state; default: return state; }}
export const pl4range25R = (state = true, action) => { switch(action.type) { case 'PL4_25': return !state; default: return state; }}
export const pl4range26R = (state = true, action) => { switch(action.type) { case 'PL4_26': return !state; default: return state; }}
export const pl4range27R = (state = true, action) => { switch(action.type) { case 'PL4_27': return !state; default: return state; }}
export const pl4range28R = (state = true, action) => { switch(action.type) { case 'PL4_28': return !state; default: return state; }}
export const pl4range29R = (state = true, action) => { switch(action.type) { case 'PL4_29': return !state; default: return state; }}
export const pl4range30R = (state = true, action) => { switch(action.type) { case 'PL4_30': return !state; default: return state; }}
export const pl4range31R = (state = true, action) => { switch(action.type) { case 'PL4_31': return !state; default: return state; }}
export const pl4range32R = (state = true, action) => { switch(action.type) { case 'PL4_32': return !state; default: return state; }}
export const pl4range33R = (state = true, action) => { switch(action.type) { case 'PL4_33': return !state; default: return state; }}
export const pl4range34R = (state = true, action) => { switch(action.type) { case 'PL4_34': return !state; default: return state; }}
export const pl4range35R = (state = true, action) => { switch(action.type) { case 'PL4_35': return !state; default: return state; }}
export const pl4range36R = (state = true, action) => { switch(action.type) { case 'PL4_36': return !state; default: return state; }}
export const pl4range37R = (state = true, action) => { switch(action.type) { case 'PL4_37': return !state; default: return state; }}
export const pl4range38R = (state = true, action) => { switch(action.type) { case 'PL4_38': return !state; default: return state; }}
export const pl4range39R = (state = true, action) => { switch(action.type) { case 'PL4_39': return !state; default: return state; }}
export const pl4range40R = (state = true, action) => { switch(action.type) { case 'PL4_40': return !state; default: return state; }}
export const pl4range41R = (state = true, action) => { switch(action.type) { case 'PL4_41': return !state; default: return state; }}
export const pl4range42R = (state = true, action) => { switch(action.type) { case 'PL4_42': return !state; default: return state; }}
export const pl4range43R = (state = true, action) => { switch(action.type) { case 'PL4_43': return !state; default: return state; }}
export const pl4range44R = (state = true, action) => { switch(action.type) { case 'PL4_44': return !state; default: return state; }}
export const pl4range45R = (state = true, action) => { switch(action.type) { case 'PL4_45': return !state; default: return state; }}
export const pl4range46R = (state = true, action) => { switch(action.type) { case 'PL4_46': return !state; default: return state; }}
export const pl4range47R = (state = true, action) => { switch(action.type) { case 'PL4_47': return !state; default: return state; }}
export const pl4range48R = (state = true, action) => { switch(action.type) { case 'PL4_48': return !state; default: return state; }}
export const pl4range49R = (state = true, action) => { switch(action.type) { case 'PL4_49': return !state; default: return state; }}
export const pl4range50R = (state = true, action) => { switch(action.type) { case 'PL4_50': return !state; default: return state; }}
export const pl4range51R = (state = true, action) => { switch(action.type) { case 'PL4_51': return !state; default: return state; }}
export const pl4range52R = (state = true, action) => { switch(action.type) { case 'PL4_52': return !state; default: return state; }}
export const pl4range53R = (state = true, action) => { switch(action.type) { case 'PL4_53': return !state; default: return state; }}
export const pl4range54R = (state = true, action) => { switch(action.type) { case 'PL4_54': return !state; default: return state; }}
export const pl4range55R = (state = true, action) => { switch(action.type) { case 'PL4_55': return !state; default: return state; }}
export const pl4range56R = (state = true, action) => { switch(action.type) { case 'PL4_56': return !state; default: return state; }}
export const pl4range57R = (state = true, action) => { switch(action.type) { case 'PL4_57': return !state; default: return state; }}
export const pl4range58R = (state = true, action) => { switch(action.type) { case 'PL4_58': return !state; default: return state; }}
export const pl4range59R = (state = true, action) => { switch(action.type) { case 'PL4_59': return !state; default: return state; }}
export const pl4range60R = (state = true, action) => { switch(action.type) { case 'PL4_60': return !state; default: return state; }}
export const pl4range61R = (state = true, action) => { switch(action.type) { case 'PL4_61': return !state; default: return state; }}
export const pl4range62R = (state = true, action) => { switch(action.type) { case 'PL4_62': return !state; default: return state; }}
export const pl4range63R = (state = true, action) => { switch(action.type) { case 'PL4_63': return !state; default: return state; }}
export const pl4range64R = (state = true, action) => { switch(action.type) { case 'PL4_64': return !state; default: return state; }}
export const pl4range65R = (state = true, action) => { switch(action.type) { case 'PL4_65': return !state; default: return state; }}
export const pl4range66R = (state = true, action) => { switch(action.type) { case 'PL4_66': return !state; default: return state; }}
export const pl4range67R = (state = true, action) => { switch(action.type) { case 'PL4_67': return !state; default: return state; }}
export const pl4range68R = (state = true, action) => { switch(action.type) { case 'PL4_68': return !state; default: return state; }}
export const pl4range69R = (state = true, action) => { switch(action.type) { case 'PL4_69': return !state; default: return state; }}
export const pl4range70R = (state = true, action) => { switch(action.type) { case 'PL4_70': return !state; default: return state; }}
export const pl4range71R = (state = true, action) => { switch(action.type) { case 'PL4_71': return !state; default: return state; }}
export const pl4range72R = (state = true, action) => { switch(action.type) { case 'PL4_72': return !state; default: return state; }}
export const pl4range73R = (state = true, action) => { switch(action.type) { case 'PL4_73': return !state; default: return state; }}
export const pl4range74R = (state = true, action) => { switch(action.type) { case 'PL4_74': return !state; default: return state; }}
export const pl4range75R = (state = true, action) => { switch(action.type) { case 'PL4_75': return !state; default: return state; }}
export const pl4range76R = (state = true, action) => { switch(action.type) { case 'PL4_76': return !state; default: return state; }}
export const pl4range77R = (state = true, action) => { switch(action.type) { case 'PL4_77': return !state; default: return state; }}
export const pl4range78R = (state = true, action) => { switch(action.type) { case 'PL4_78': return !state; default: return state; }}
export const pl4range79R = (state = true, action) => { switch(action.type) { case 'PL4_79': return !state; default: return state; }}
export const pl4range80R = (state = true, action) => { switch(action.type) { case 'PL4_80': return !state; default: return state; }}
export const pl4range81R = (state = true, action) => { switch(action.type) { case 'PL4_81': return !state; default: return state; }}
export const pl4range82R = (state = true, action) => { switch(action.type) { case 'PL4_82': return !state; default: return state; }}
export const pl4range83R = (state = true, action) => { switch(action.type) { case 'PL4_83': return !state; default: return state; }}
export const pl4range84R = (state = true, action) => { switch(action.type) { case 'PL4_84': return !state; default: return state; }}
export const pl4range85R = (state = true, action) => { switch(action.type) { case 'PL4_85': return !state; default: return state; }}
export const pl4range86R = (state = true, action) => { switch(action.type) { case 'PL4_86': return !state; default: return state; }}
export const pl4range87R = (state = true, action) => { switch(action.type) { case 'PL4_87': return !state; default: return state; }}
export const pl4range88R = (state = true, action) => { switch(action.type) { case 'PL4_88': return !state; default: return state; }}
export const pl4range89R = (state = true, action) => { switch(action.type) { case 'PL4_89': return !state; default: return state; }}
export const pl4range90R = (state = true, action) => { switch(action.type) { case 'PL4_90': return !state; default: return state; }}
export const pl4range91R = (state = true, action) => { switch(action.type) { case 'PL4_91': return !state; default: return state; }}
export const pl4range92R = (state = true, action) => { switch(action.type) { case 'PL4_92': return !state; default: return state; }}
export const pl4range93R = (state = true, action) => { switch(action.type) { case 'PL4_93': return !state; default: return state; }}
export const pl4range94R = (state = true, action) => { switch(action.type) { case 'PL4_94': return !state; default: return state; }}
export const pl4range95R = (state = true, action) => { switch(action.type) { case 'PL4_95': return !state; default: return state; }}
export const pl4range96R = (state = true, action) => { switch(action.type) { case 'PL4_96': return !state; default: return state; }}
export const pl4range97R = (state = true, action) => { switch(action.type) { case 'PL4_97': return !state; default: return state; }}
export const pl4range98R = (state = true, action) => { switch(action.type) { case 'PL4_98': return !state; default: return state; }}
export const pl4range99R = (state = true, action) => { switch(action.type) { case 'PL4_99': return !state; default: return state; }}
export const pl4range100R = (state = true, action) => { switch(action.type) { case 'PL4_100': return !state; default: return state; }}
export const pl4range101R = (state = true, action) => { switch(action.type) { case 'PL4_101': return !state; default: return state; }}
export const pl4range102R = (state = true, action) => { switch(action.type) { case 'PL4_102': return !state; default: return state; }}
export const pl4range103R = (state = true, action) => { switch(action.type) { case 'PL4_103': return !state; default: return state; }}
export const pl4range104R = (state = true, action) => { switch(action.type) { case 'PL4_104': return !state; default: return state; }}
export const pl4range105R = (state = true, action) => { switch(action.type) { case 'PL4_105': return !state; default: return state; }}
export const pl4range106R = (state = true, action) => { switch(action.type) { case 'PL4_106': return !state; default: return state; }}
export const pl4range107R = (state = true, action) => { switch(action.type) { case 'PL4_107': return !state; default: return state; }}
export const pl4range108R = (state = true, action) => { switch(action.type) { case 'PL4_108': return !state; default: return state; }}
export const pl4range109R = (state = true, action) => { switch(action.type) { case 'PL4_109': return !state; default: return state; }}
export const pl4range110R = (state = true, action) => { switch(action.type) { case 'PL4_110': return !state; default: return state; }}
export const pl4range111R = (state = true, action) => { switch(action.type) { case 'PL4_111': return !state; default: return state; }}
export const pl4range112R = (state = true, action) => { switch(action.type) { case 'PL4_112': return !state; default: return state; }}
export const pl4range113R = (state = true, action) => { switch(action.type) { case 'PL4_113': return !state; default: return state; }}
export const pl4range114R = (state = true, action) => { switch(action.type) { case 'PL4_114': return !state; default: return state; }}
export const pl4range115R = (state = true, action) => { switch(action.type) { case 'PL4_115': return !state; default: return state; }}
export const pl4range116R = (state = true, action) => { switch(action.type) { case 'PL4_116': return !state; default: return state; }}
export const pl4range117R = (state = true, action) => { switch(action.type) { case 'PL4_117': return !state; default: return state; }}
export const pl4range118R = (state = true, action) => { switch(action.type) { case 'PL4_118': return !state; default: return state; }}
export const pl4range119R = (state = true, action) => { switch(action.type) { case 'PL4_119': return !state; default: return state; }}
export const pl4range120R = (state = true, action) => { switch(action.type) { case 'PL4_120': return !state; default: return state; }}
export const pl4range121R = (state = true, action) => { switch(action.type) { case 'PL4_121': return !state; default: return state; }}
export const pl4range122R = (state = true, action) => { switch(action.type) { case 'PL4_122': return !state; default: return state; }}
export const pl4range123R = (state = true, action) => { switch(action.type) { case 'PL4_123': return !state; default: return state; }}
export const pl4range124R = (state = true, action) => { switch(action.type) { case 'PL4_124': return !state; default: return state; }}
export const pl4range125R = (state = true, action) => { switch(action.type) { case 'PL4_125': return !state; default: return state; }}
export const pl4range126R = (state = true, action) => { switch(action.type) { case 'PL4_126': return !state; default: return state; }}
export const pl4range127R = (state = true, action) => { switch(action.type) { case 'PL4_127': return !state; default: return state; }}
export const pl4range128R = (state = true, action) => { switch(action.type) { case 'PL4_128': return !state; default: return state; }}
export const pl4range129R = (state = true, action) => { switch(action.type) { case 'PL4_129': return !state; default: return state; }}
export const pl4range130R = (state = true, action) => { switch(action.type) { case 'PL4_130': return !state; default: return state; }}
export const pl4range131R = (state = true, action) => { switch(action.type) { case 'PL4_131': return !state; default: return state; }}
export const pl4range132R = (state = true, action) => { switch(action.type) { case 'PL4_132': return !state; default: return state; }}
export const pl4range133R = (state = true, action) => { switch(action.type) { case 'PL4_133': return !state; default: return state; }}
export const pl4range134R = (state = true, action) => { switch(action.type) { case 'PL4_134': return !state; default: return state; }}
export const pl4range135R = (state = true, action) => { switch(action.type) { case 'PL4_135': return !state; default: return state; }}
export const pl4range136R = (state = true, action) => { switch(action.type) { case 'PL4_136': return !state; default: return state; }}
export const pl4range137R = (state = true, action) => { switch(action.type) { case 'PL4_137': return !state; default: return state; }}
export const pl4range138R = (state = true, action) => { switch(action.type) { case 'PL4_138': return !state; default: return state; }}
export const pl4range139R = (state = true, action) => { switch(action.type) { case 'PL4_139': return !state; default: return state; }}
export const pl4range140R = (state = true, action) => { switch(action.type) { case 'PL4_140': return !state; default: return state; }}
export const pl4range141R = (state = true, action) => { switch(action.type) { case 'PL4_141': return !state; default: return state; }}
export const pl4range142R = (state = true, action) => { switch(action.type) { case 'PL4_142': return !state; default: return state; }}
export const pl4range143R = (state = true, action) => { switch(action.type) { case 'PL4_143': return !state; default: return state; }}
export const pl4range144R = (state = true, action) => { switch(action.type) { case 'PL4_144': return !state; default: return state; }}
export const pl4range145R = (state = true, action) => { switch(action.type) { case 'PL4_145': return !state; default: return state; }}
export const pl4range146R = (state = true, action) => { switch(action.type) { case 'PL4_146': return !state; default: return state; }}
export const pl4range147R = (state = true, action) => { switch(action.type) { case 'PL4_147': return !state; default: return state; }}
export const pl4range148R = (state = true, action) => { switch(action.type) { case 'PL4_148': return !state; default: return state; }}
export const pl4range149R = (state = true, action) => { switch(action.type) { case 'PL4_149': return !state; default: return state; }}
export const pl4range150R = (state = true, action) => { switch(action.type) { case 'PL4_150': return !state; default: return state; }}
export const pl4range151R = (state = true, action) => { switch(action.type) { case 'PL4_151': return !state; default: return state; }}
export const pl4range152R = (state = true, action) => { switch(action.type) { case 'PL4_152': return !state; default: return state; }}
export const pl4range153R = (state = true, action) => { switch(action.type) { case 'PL4_153': return !state; default: return state; }}
export const pl4range154R = (state = true, action) => { switch(action.type) { case 'PL4_154': return !state; default: return state; }}
export const pl4range155R = (state = true, action) => { switch(action.type) { case 'PL4_155': return !state; default: return state; }}
export const pl4range156R = (state = true, action) => { switch(action.type) { case 'PL4_156': return !state; default: return state; }}
export const pl4range157R = (state = true, action) => { switch(action.type) { case 'PL4_157': return !state; default: return state; }}
export const pl4range158R = (state = true, action) => { switch(action.type) { case 'PL4_158': return !state; default: return state; }}
export const pl4range159R = (state = true, action) => { switch(action.type) { case 'PL4_159': return !state; default: return state; }}
export const pl4range160R = (state = true, action) => { switch(action.type) { case 'PL4_160': return !state; default: return state; }}
export const pl4range161R = (state = true, action) => { switch(action.type) { case 'PL4_161': return !state; default: return state; }}
export const pl4range162R = (state = true, action) => { switch(action.type) { case 'PL4_162': return !state; default: return state; }}
export const pl4range163R = (state = true, action) => { switch(action.type) { case 'PL4_163': return !state; default: return state; }}
export const pl4range164R = (state = true, action) => { switch(action.type) { case 'PL4_164': return !state; default: return state; }}
export const pl4range165R = (state = true, action) => { switch(action.type) { case 'PL4_165': return !state; default: return state; }}
export const pl4range166R = (state = true, action) => { switch(action.type) { case 'PL4_166': return !state; default: return state; }}
export const pl4range167R = (state = true, action) => { switch(action.type) { case 'PL4_167': return !state; default: return state; }}
export const pl4range168R = (state = true, action) => { switch(action.type) { case 'PL4_168': return !state; default: return state; }}
export const pl4range169R = (state = true, action) => { switch(action.type) { case 'PL4_169': return !state; default: return state; }}

export const pl5range1R = (state = true, action) => { switch(action.type) { case 'PL5_1': return !state; default: return state; }}
export const pl5range2R = (state = true, action) => { switch(action.type) { case 'PL5_2': return !state; default: return state; }}
export const pl5range3R = (state = true, action) => { switch(action.type) { case 'PL5_3': return !state; default: return state; }}
export const pl5range4R = (state = true, action) => { switch(action.type) { case 'PL5_4': return !state; default: return state; }}
export const pl5range5R = (state = true, action) => { switch(action.type) { case 'PL5_5': return !state; default: return state; }}
export const pl5range6R = (state = true, action) => { switch(action.type) { case 'PL5_6': return !state; default: return state; }}
export const pl5range7R = (state = true, action) => { switch(action.type) { case 'PL5_7': return !state; default: return state; }}
export const pl5range8R = (state = true, action) => { switch(action.type) { case 'PL5_8': return !state; default: return state; }}
export const pl5range9R = (state = true, action) => { switch(action.type) { case 'PL5_9': return !state; default: return state; }}
export const pl5range10R = (state = true, action) => { switch(action.type) { case 'PL5_10': return !state; default: return state; }}
export const pl5range11R = (state = true, action) => { switch(action.type) { case 'PL5_11': return !state; default: return state; }}
export const pl5range12R = (state = true, action) => { switch(action.type) { case 'PL5_12': return !state; default: return state; }}
export const pl5range13R = (state = true, action) => { switch(action.type) { case 'PL5_13': return !state; default: return state; }}
export const pl5range14R = (state = true, action) => { switch(action.type) { case 'PL5_14': return !state; default: return state; }}
export const pl5range15R = (state = true, action) => { switch(action.type) { case 'PL5_15': return !state; default: return state; }}
export const pl5range16R = (state = true, action) => { switch(action.type) { case 'PL5_16': return !state; default: return state; }}
export const pl5range17R = (state = true, action) => { switch(action.type) { case 'PL5_17': return !state; default: return state; }}
export const pl5range18R = (state = true, action) => { switch(action.type) { case 'PL5_18': return !state; default: return state; }}
export const pl5range19R = (state = true, action) => { switch(action.type) { case 'PL5_19': return !state; default: return state; }}
export const pl5range20R = (state = true, action) => { switch(action.type) { case 'PL5_20': return !state; default: return state; }}
export const pl5range21R = (state = true, action) => { switch(action.type) { case 'PL5_21': return !state; default: return state; }}
export const pl5range22R = (state = true, action) => { switch(action.type) { case 'PL5_22': return !state; default: return state; }}
export const pl5range23R = (state = true, action) => { switch(action.type) { case 'PL5_23': return !state; default: return state; }}
export const pl5range24R = (state = true, action) => { switch(action.type) { case 'PL5_24': return !state; default: return state; }}
export const pl5range25R = (state = true, action) => { switch(action.type) { case 'PL5_25': return !state; default: return state; }}
export const pl5range26R = (state = true, action) => { switch(action.type) { case 'PL5_26': return !state; default: return state; }}
export const pl5range27R = (state = true, action) => { switch(action.type) { case 'PL5_27': return !state; default: return state; }}
export const pl5range28R = (state = true, action) => { switch(action.type) { case 'PL5_28': return !state; default: return state; }}
export const pl5range29R = (state = true, action) => { switch(action.type) { case 'PL5_29': return !state; default: return state; }}
export const pl5range30R = (state = true, action) => { switch(action.type) { case 'PL5_30': return !state; default: return state; }}
export const pl5range31R = (state = true, action) => { switch(action.type) { case 'PL5_31': return !state; default: return state; }}
export const pl5range32R = (state = true, action) => { switch(action.type) { case 'PL5_32': return !state; default: return state; }}
export const pl5range33R = (state = true, action) => { switch(action.type) { case 'PL5_33': return !state; default: return state; }}
export const pl5range34R = (state = true, action) => { switch(action.type) { case 'PL5_34': return !state; default: return state; }}
export const pl5range35R = (state = true, action) => { switch(action.type) { case 'PL5_35': return !state; default: return state; }}
export const pl5range36R = (state = true, action) => { switch(action.type) { case 'PL5_36': return !state; default: return state; }}
export const pl5range37R = (state = true, action) => { switch(action.type) { case 'PL5_37': return !state; default: return state; }}
export const pl5range38R = (state = true, action) => { switch(action.type) { case 'PL5_38': return !state; default: return state; }}
export const pl5range39R = (state = true, action) => { switch(action.type) { case 'PL5_39': return !state; default: return state; }}
export const pl5range40R = (state = true, action) => { switch(action.type) { case 'PL5_40': return !state; default: return state; }}
export const pl5range41R = (state = true, action) => { switch(action.type) { case 'PL5_41': return !state; default: return state; }}
export const pl5range42R = (state = true, action) => { switch(action.type) { case 'PL5_42': return !state; default: return state; }}
export const pl5range43R = (state = true, action) => { switch(action.type) { case 'PL5_43': return !state; default: return state; }}
export const pl5range44R = (state = true, action) => { switch(action.type) { case 'PL5_44': return !state; default: return state; }}
export const pl5range45R = (state = true, action) => { switch(action.type) { case 'PL5_45': return !state; default: return state; }}
export const pl5range46R = (state = true, action) => { switch(action.type) { case 'PL5_46': return !state; default: return state; }}
export const pl5range47R = (state = true, action) => { switch(action.type) { case 'PL5_47': return !state; default: return state; }}
export const pl5range48R = (state = true, action) => { switch(action.type) { case 'PL5_48': return !state; default: return state; }}
export const pl5range49R = (state = true, action) => { switch(action.type) { case 'PL5_49': return !state; default: return state; }}
export const pl5range50R = (state = true, action) => { switch(action.type) { case 'PL5_50': return !state; default: return state; }}
export const pl5range51R = (state = true, action) => { switch(action.type) { case 'PL5_51': return !state; default: return state; }}
export const pl5range52R = (state = true, action) => { switch(action.type) { case 'PL5_52': return !state; default: return state; }}
export const pl5range53R = (state = true, action) => { switch(action.type) { case 'PL5_53': return !state; default: return state; }}
export const pl5range54R = (state = true, action) => { switch(action.type) { case 'PL5_54': return !state; default: return state; }}
export const pl5range55R = (state = true, action) => { switch(action.type) { case 'PL5_55': return !state; default: return state; }}
export const pl5range56R = (state = true, action) => { switch(action.type) { case 'PL5_56': return !state; default: return state; }}
export const pl5range57R = (state = true, action) => { switch(action.type) { case 'PL5_57': return !state; default: return state; }}
export const pl5range58R = (state = true, action) => { switch(action.type) { case 'PL5_58': return !state; default: return state; }}
export const pl5range59R = (state = true, action) => { switch(action.type) { case 'PL5_59': return !state; default: return state; }}
export const pl5range60R = (state = true, action) => { switch(action.type) { case 'PL5_60': return !state; default: return state; }}
export const pl5range61R = (state = true, action) => { switch(action.type) { case 'PL5_61': return !state; default: return state; }}
export const pl5range62R = (state = true, action) => { switch(action.type) { case 'PL5_62': return !state; default: return state; }}
export const pl5range63R = (state = true, action) => { switch(action.type) { case 'PL5_63': return !state; default: return state; }}
export const pl5range64R = (state = true, action) => { switch(action.type) { case 'PL5_64': return !state; default: return state; }}
export const pl5range65R = (state = true, action) => { switch(action.type) { case 'PL5_65': return !state; default: return state; }}
export const pl5range66R = (state = true, action) => { switch(action.type) { case 'PL5_66': return !state; default: return state; }}
export const pl5range67R = (state = true, action) => { switch(action.type) { case 'PL5_67': return !state; default: return state; }}
export const pl5range68R = (state = true, action) => { switch(action.type) { case 'PL5_68': return !state; default: return state; }}
export const pl5range69R = (state = true, action) => { switch(action.type) { case 'PL5_69': return !state; default: return state; }}
export const pl5range70R = (state = true, action) => { switch(action.type) { case 'PL5_70': return !state; default: return state; }}
export const pl5range71R = (state = true, action) => { switch(action.type) { case 'PL5_71': return !state; default: return state; }}
export const pl5range72R = (state = true, action) => { switch(action.type) { case 'PL5_72': return !state; default: return state; }}
export const pl5range73R = (state = true, action) => { switch(action.type) { case 'PL5_73': return !state; default: return state; }}
export const pl5range74R = (state = true, action) => { switch(action.type) { case 'PL5_74': return !state; default: return state; }}
export const pl5range75R = (state = true, action) => { switch(action.type) { case 'PL5_75': return !state; default: return state; }}
export const pl5range76R = (state = true, action) => { switch(action.type) { case 'PL5_76': return !state; default: return state; }}
export const pl5range77R = (state = true, action) => { switch(action.type) { case 'PL5_77': return !state; default: return state; }}
export const pl5range78R = (state = true, action) => { switch(action.type) { case 'PL5_78': return !state; default: return state; }}
export const pl5range79R = (state = true, action) => { switch(action.type) { case 'PL5_79': return !state; default: return state; }}
export const pl5range80R = (state = true, action) => { switch(action.type) { case 'PL5_80': return !state; default: return state; }}
export const pl5range81R = (state = true, action) => { switch(action.type) { case 'PL5_81': return !state; default: return state; }}
export const pl5range82R = (state = true, action) => { switch(action.type) { case 'PL5_82': return !state; default: return state; }}
export const pl5range83R = (state = true, action) => { switch(action.type) { case 'PL5_83': return !state; default: return state; }}
export const pl5range84R = (state = true, action) => { switch(action.type) { case 'PL5_84': return !state; default: return state; }}
export const pl5range85R = (state = true, action) => { switch(action.type) { case 'PL5_85': return !state; default: return state; }}
export const pl5range86R = (state = true, action) => { switch(action.type) { case 'PL5_86': return !state; default: return state; }}
export const pl5range87R = (state = true, action) => { switch(action.type) { case 'PL5_87': return !state; default: return state; }}
export const pl5range88R = (state = true, action) => { switch(action.type) { case 'PL5_88': return !state; default: return state; }}
export const pl5range89R = (state = true, action) => { switch(action.type) { case 'PL5_89': return !state; default: return state; }}
export const pl5range90R = (state = true, action) => { switch(action.type) { case 'PL5_90': return !state; default: return state; }}
export const pl5range91R = (state = true, action) => { switch(action.type) { case 'PL5_91': return !state; default: return state; }}
export const pl5range92R = (state = true, action) => { switch(action.type) { case 'PL5_92': return !state; default: return state; }}
export const pl5range93R = (state = true, action) => { switch(action.type) { case 'PL5_93': return !state; default: return state; }}
export const pl5range94R = (state = true, action) => { switch(action.type) { case 'PL5_94': return !state; default: return state; }}
export const pl5range95R = (state = true, action) => { switch(action.type) { case 'PL5_95': return !state; default: return state; }}
export const pl5range96R = (state = true, action) => { switch(action.type) { case 'PL5_96': return !state; default: return state; }}
export const pl5range97R = (state = true, action) => { switch(action.type) { case 'PL5_97': return !state; default: return state; }}
export const pl5range98R = (state = true, action) => { switch(action.type) { case 'PL5_98': return !state; default: return state; }}
export const pl5range99R = (state = true, action) => { switch(action.type) { case 'PL5_99': return !state; default: return state; }}
export const pl5range100R = (state = true, action) => { switch(action.type) { case 'PL5_100': return !state; default: return state; }}
export const pl5range101R = (state = true, action) => { switch(action.type) { case 'PL5_101': return !state; default: return state; }}
export const pl5range102R = (state = true, action) => { switch(action.type) { case 'PL5_102': return !state; default: return state; }}
export const pl5range103R = (state = true, action) => { switch(action.type) { case 'PL5_103': return !state; default: return state; }}
export const pl5range104R = (state = true, action) => { switch(action.type) { case 'PL5_104': return !state; default: return state; }}
export const pl5range105R = (state = true, action) => { switch(action.type) { case 'PL5_105': return !state; default: return state; }}
export const pl5range106R = (state = true, action) => { switch(action.type) { case 'PL5_106': return !state; default: return state; }}
export const pl5range107R = (state = true, action) => { switch(action.type) { case 'PL5_107': return !state; default: return state; }}
export const pl5range108R = (state = true, action) => { switch(action.type) { case 'PL5_108': return !state; default: return state; }}
export const pl5range109R = (state = true, action) => { switch(action.type) { case 'PL5_109': return !state; default: return state; }}
export const pl5range110R = (state = true, action) => { switch(action.type) { case 'PL5_110': return !state; default: return state; }}
export const pl5range111R = (state = true, action) => { switch(action.type) { case 'PL5_111': return !state; default: return state; }}
export const pl5range112R = (state = true, action) => { switch(action.type) { case 'PL5_112': return !state; default: return state; }}
export const pl5range113R = (state = true, action) => { switch(action.type) { case 'PL5_113': return !state; default: return state; }}
export const pl5range114R = (state = true, action) => { switch(action.type) { case 'PL5_114': return !state; default: return state; }}
export const pl5range115R = (state = true, action) => { switch(action.type) { case 'PL5_115': return !state; default: return state; }}
export const pl5range116R = (state = true, action) => { switch(action.type) { case 'PL5_116': return !state; default: return state; }}
export const pl5range117R = (state = true, action) => { switch(action.type) { case 'PL5_117': return !state; default: return state; }}
export const pl5range118R = (state = true, action) => { switch(action.type) { case 'PL5_118': return !state; default: return state; }}
export const pl5range119R = (state = true, action) => { switch(action.type) { case 'PL5_119': return !state; default: return state; }}
export const pl5range120R = (state = true, action) => { switch(action.type) { case 'PL5_120': return !state; default: return state; }}
export const pl5range121R = (state = true, action) => { switch(action.type) { case 'PL5_121': return !state; default: return state; }}
export const pl5range122R = (state = true, action) => { switch(action.type) { case 'PL5_122': return !state; default: return state; }}
export const pl5range123R = (state = true, action) => { switch(action.type) { case 'PL5_123': return !state; default: return state; }}
export const pl5range124R = (state = true, action) => { switch(action.type) { case 'PL5_124': return !state; default: return state; }}
export const pl5range125R = (state = true, action) => { switch(action.type) { case 'PL5_125': return !state; default: return state; }}
export const pl5range126R = (state = true, action) => { switch(action.type) { case 'PL5_126': return !state; default: return state; }}
export const pl5range127R = (state = true, action) => { switch(action.type) { case 'PL5_127': return !state; default: return state; }}
export const pl5range128R = (state = true, action) => { switch(action.type) { case 'PL5_128': return !state; default: return state; }}
export const pl5range129R = (state = true, action) => { switch(action.type) { case 'PL5_129': return !state; default: return state; }}
export const pl5range130R = (state = true, action) => { switch(action.type) { case 'PL5_130': return !state; default: return state; }}
export const pl5range131R = (state = true, action) => { switch(action.type) { case 'PL5_131': return !state; default: return state; }}
export const pl5range132R = (state = true, action) => { switch(action.type) { case 'PL5_132': return !state; default: return state; }}
export const pl5range133R = (state = true, action) => { switch(action.type) { case 'PL5_133': return !state; default: return state; }}
export const pl5range134R = (state = true, action) => { switch(action.type) { case 'PL5_134': return !state; default: return state; }}
export const pl5range135R = (state = true, action) => { switch(action.type) { case 'PL5_135': return !state; default: return state; }}
export const pl5range136R = (state = true, action) => { switch(action.type) { case 'PL5_136': return !state; default: return state; }}
export const pl5range137R = (state = true, action) => { switch(action.type) { case 'PL5_137': return !state; default: return state; }}
export const pl5range138R = (state = true, action) => { switch(action.type) { case 'PL5_138': return !state; default: return state; }}
export const pl5range139R = (state = true, action) => { switch(action.type) { case 'PL5_139': return !state; default: return state; }}
export const pl5range140R = (state = true, action) => { switch(action.type) { case 'PL5_140': return !state; default: return state; }}
export const pl5range141R = (state = true, action) => { switch(action.type) { case 'PL5_141': return !state; default: return state; }}
export const pl5range142R = (state = true, action) => { switch(action.type) { case 'PL5_142': return !state; default: return state; }}
export const pl5range143R = (state = true, action) => { switch(action.type) { case 'PL5_143': return !state; default: return state; }}
export const pl5range144R = (state = true, action) => { switch(action.type) { case 'PL5_144': return !state; default: return state; }}
export const pl5range145R = (state = true, action) => { switch(action.type) { case 'PL5_145': return !state; default: return state; }}
export const pl5range146R = (state = true, action) => { switch(action.type) { case 'PL5_146': return !state; default: return state; }}
export const pl5range147R = (state = true, action) => { switch(action.type) { case 'PL5_147': return !state; default: return state; }}
export const pl5range148R = (state = true, action) => { switch(action.type) { case 'PL5_148': return !state; default: return state; }}
export const pl5range149R = (state = true, action) => { switch(action.type) { case 'PL5_149': return !state; default: return state; }}
export const pl5range150R = (state = true, action) => { switch(action.type) { case 'PL5_150': return !state; default: return state; }}
export const pl5range151R = (state = true, action) => { switch(action.type) { case 'PL5_151': return !state; default: return state; }}
export const pl5range152R = (state = true, action) => { switch(action.type) { case 'PL5_152': return !state; default: return state; }}
export const pl5range153R = (state = true, action) => { switch(action.type) { case 'PL5_153': return !state; default: return state; }}
export const pl5range154R = (state = true, action) => { switch(action.type) { case 'PL5_154': return !state; default: return state; }}
export const pl5range155R = (state = true, action) => { switch(action.type) { case 'PL5_155': return !state; default: return state; }}
export const pl5range156R = (state = true, action) => { switch(action.type) { case 'PL5_156': return !state; default: return state; }}
export const pl5range157R = (state = true, action) => { switch(action.type) { case 'PL5_157': return !state; default: return state; }}
export const pl5range158R = (state = true, action) => { switch(action.type) { case 'PL5_158': return !state; default: return state; }}
export const pl5range159R = (state = true, action) => { switch(action.type) { case 'PL5_159': return !state; default: return state; }}
export const pl5range160R = (state = true, action) => { switch(action.type) { case 'PL5_160': return !state; default: return state; }}
export const pl5range161R = (state = true, action) => { switch(action.type) { case 'PL5_161': return !state; default: return state; }}
export const pl5range162R = (state = true, action) => { switch(action.type) { case 'PL5_162': return !state; default: return state; }}
export const pl5range163R = (state = true, action) => { switch(action.type) { case 'PL5_163': return !state; default: return state; }}
export const pl5range164R = (state = true, action) => { switch(action.type) { case 'PL5_164': return !state; default: return state; }}
export const pl5range165R = (state = true, action) => { switch(action.type) { case 'PL5_165': return !state; default: return state; }}
export const pl5range166R = (state = true, action) => { switch(action.type) { case 'PL5_166': return !state; default: return state; }}
export const pl5range167R = (state = true, action) => { switch(action.type) { case 'PL5_167': return !state; default: return state; }}
export const pl5range168R = (state = true, action) => { switch(action.type) { case 'PL5_168': return !state; default: return state; }}
export const pl5range169R = (state = true, action) => { switch(action.type) { case 'PL5_169': return !state; default: return state; }}